import React from 'react';

class TesteButton extends React.Component {
    render(){
        return(
            <button>
                Button
            </button>
        )
    }
}

export default TesteButton